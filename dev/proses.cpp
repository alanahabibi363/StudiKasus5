#include "../base.h"
using namespace std;

void krs::proses(){
  int jml_matkul, n;
  int diskon,jml_sks, jml_bayar;

  total_sks = 
  sks = 3;
  ttl_sks = sks * harga_sks;
  total = 450000;
  diskon = total*0.25;
  jml_bayar = total - diskon;

  total = ((sks * 150000) * 25/100 ;
  if (total>30000){
    diskon = total * 0.1;
    bayar = total - diskon;
      }
    
      else{
      diskon = 0;
      bayar = total;
      }  

  jum_sks=0;
  bayar = 0;
  
  
  
  }
int main ()
{
  krs kuliah;
  kuliah.proses();
}